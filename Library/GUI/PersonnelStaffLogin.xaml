<Window x:Class="Library.GUI.PersonnelStaffLogin"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Library.GUI"
        mc:Ignorable="d"
        Title="PersonnelStaffLogin" Height="450" Width="800">
    <Window.Resources>
        <Style TargetType="TextBox" >
            <Setter Property="BorderThickness" Value="0,0,0,2" />
            <Setter Property="BorderBrush" Value="Gray" />
            <Setter Property="Padding" Value="5" />
            <Setter Property="Margin" Value="0,0,0,10" />
            <Setter Property="Background" Value="Transparent" />
            <Style.Triggers>
                <Trigger Property="IsKeyboardFocusWithin" Value="True">
                    <Setter Property="BorderBrush" Value="Blue" />
                </Trigger>
            </Style.Triggers>
        </Style>
        <Style TargetType="Label">
            <Setter Property="Foreground" Value="Gray" />
            <Setter Property="Margin" Value="0,0,0,5" />
        </Style>
        <Style TargetType="Button">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="ButtonBorder" Background="White" BorderBrush="#2689ac" BorderThickness="1" CornerRadius="10">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="ButtonBorder" Property="Background" Value="#2689ac"/>
                                <Setter TargetName="ButtonBorder" Property="BorderBrush" Value="#2689ac"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style TargetType="PasswordBox" >
            <Setter Property="BorderThickness" Value="0,0,0,2" />
            <Setter Property="BorderBrush" Value="Gray" />
            <Setter Property="Padding" Value="5" />
            <Setter Property="Margin" Value="0,0,0,10" />
            <Setter Property="Background" Value="Transparent" />
            <Style.Triggers>
                <Trigger Property="IsKeyboardFocusWithin" Value="True">
                    <Setter Property="BorderBrush" Value="Blue" />
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*" />
            <ColumnDefinition Width="3*" />
        </Grid.ColumnDefinitions>

        <!-- Partie de gauche avec la phrase et les champs d'entrée et le bouton -->
        <StackPanel Grid.Column="0" Margin="20">
            <TextBlock Text="Cet espace est réservé au personnel" FontSize="16" FontWeight="Bold" Margin="0,0,0,20"/>

            <Label Content="Identifiant" />
            <TextBox Name="UsernameTxt" TextChanged="UsernameTxt_TextChanged" />
            <Label Content="Mot de passe" />
            <PasswordBox Name="PasswordTxt"/>
       

            <Button Content="Connecter" Margin="0,10" Click="ConnecterBtn_Click" Height="25" Width="78" />
  

        </StackPanel>

        <!-- Partie de droite avec l'image -->
        <StackPanel Grid.Column="1" VerticalAlignment="Center">
            <Image Source="C:\Users\HP\Documents\4IIR\Dot_Net\LibraryApp\Library\GUI\LibraryStaffPersonnel.jpg" 
                   Stretch="UniformToFill" VerticalAlignment="Center" Margin="0,20" />
        </StackPanel>
    </Grid>
</Window>
